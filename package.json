{
  "name": "css-timingfunction-to-react-native-easing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "rm -rf lib && babel src --out-dir lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "author": "Yusuke Shibata",
  "license": "MIT"
}
